# Senseandart Bot - README

## 🤖 Telegram Bot для сбора подписчиков в Google Sheets

Этот бот автоматически:
1. ✅ Отслеживает новых подписчиков канала @senseandart
2. ✅ Записывает их в Google Sheets таблицу
3. ✅ Отправляет приватное сообщение с кнопкой на промо-пост (#42)

---

## 📋 Структура проекта

```
senseandart-bot/
├── main.py                    # Основной код бота
├── config.py                  # Конфигурация
├── requirements.txt           # Зависимости Python
├── credentials.json           # Google Service Account ключ
├── README.md                  # Этот файл
└── deploy.sh                  # Скрипт развертывания
```

---

## 🚀 Как запустить локально (для тестирования)

### 1. Установите Python 3.9+

### 2. Создайте виртуальное окружение

```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Сохраните Google credentials

Переименуйте файл `senseandart-bot-new-ec78ea9be25f.json` в `credentials.json` и положите в папку проекта

### 5. Запустите бота

```bash
python main.py
```

Если видите сообщение "Bot is running..." — всё работает! ✅

---

## ☁️ Развертывание на Google Cloud

### 1. Установите Google Cloud SDK

https://cloud.google.com/sdk/docs/install

### 2. Авторизуйтесь в Google Cloud

```bash
gcloud auth login
gcloud config set project senseandart-bot-new
```

### 3. Запустите скрипт развертывания

```bash
bash deploy.sh
```

Скрипт автоматически:
- Создаст Cloud Run сервис
- Загрузит код бота
- Запустит бота в облаке
- Бот будет работать 24/7

---

## 📊 Google Sheets структура

Таблица должна содержать колонки:

| user_id | username | date | status |
|---------|----------|------|--------|
| 123456 | @name | 2025-12-02 12:34:56 | subscribed |
| 789012 | @other | 2025-12-02 13:45:00 | subscribed |

---

## 🔑 Конфигурация

Все настройки хранятся в `config.py`:

- `TELEGRAM_BOT_TOKEN` — токен от @BotFather
- `CHANNEL_ID` — числовой ID канала
- `GOOGLE_SHEETS_ID` — ID таблицы Google Sheets
- `SHEET_NAME` — имя листа в таблице (по умолчанию "Sheet1")
- `PROMO_POST_ID` — ID поста с промокодом (42)

---

## 🐛 Что делать если не работает?

### Бот не запускается
- Проверьте токен от @BotFather
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`

### Не логируются подписчики в Google Sheets
- Проверьте что боту дан доступ к таблице (Share → добавьте email из credentials.json)
- Проверьте что GOOGLE_SHEETS_ID правильный
- Посмотрите логи: `python main.py`

### Приватные сообщения не отправляются
- Убедитесь что пользователь не заблокировал бота
- Проверьте что PROMO_POST_ID правильный

---

## 📞 Поддержка

По вопросам обращайтесь к разработчику.

---

## 📄 Лицензия

Этот проект создан для @senseandart
